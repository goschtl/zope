# Identify the component configuration used to define the site:
site-definition /Users/ajung/src/svn.zope.org/zopyx.smartprintng.server/buildout/trunk/zopyx_smartprintng_server/parts/etc/site.zcml

<zodb>
  # Standard Filestorage
  <filestorage>
    path /Users/ajung/src/svn.zope.org/zopyx.smartprintng.server/buildout/trunk/zopyx_smartprintng_server/parts/data/Data.fs
  </filestorage>

# Uncomment this if you want to connect to a ZEO server instead:
#  <zeoclient>
#    server localhost:8100
#    storage 1
#    # ZEO client cache, in bytes
#    cache-size 20MB
#    # Uncomment to have a persistent disk cache
#    #client zeo1
#  </zeoclient>
</zodb>

<eventlog>
  # This sets up logging to a file.
  # The "path" setting can be a relative or absolute
  # filesystem path.

  <logfile>
    path /Users/ajung/src/svn.zope.org/zopyx.smartprintng.server/buildout/trunk/zopyx_smartprintng_server/parts/log/z3.log
    formatter zope.exceptions.log.Formatter
  </logfile>

  # This sets up logging to to standard output.
  # The "path" setting can be the tokens STDOUT or STDERR
  
#  <logfile>
#    path STDOUT
#    formatter zope.exceptions.log.Formatter
#  </logfile>
</eventlog>

# Comment this line to disable developer mode.  This should be done in
# production
devmode on