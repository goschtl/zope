GrokTester
----------

This tests the latest released Grok.  Especially it uses the latest
released versions file and creates a bin/test script for running the
tests of the grok egg.

Usage:

buildout bootstrap
bin/test
