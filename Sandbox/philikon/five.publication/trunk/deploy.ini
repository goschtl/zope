[composite:main]
use = egg:Paste#urlmap
/ = zope
/debug = debug

[filter-app:debug]
use = egg:z3c.evalexception
next = zope

[app:zope]
use = egg:five.publication#demo
conf = etc/zope.conf

[server:main]
use = egg:Paste#http
host = 127.0.0.1
port = 8080
